galaxy_info:
  author: "Chi Trung Nguyen"
  description: Ansible Galaxy role to install mariadb
  company: bytepark GmbH
  
  license: license (MIT)
  
  min_ansible_version: 2.0
  platforms:
  - name: Ubuntu
    versions:
     - trusty
  galaxy_tags:
    - mariadb
    - web
    - database
    - sql
    - rdbms
dependencies: []
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.